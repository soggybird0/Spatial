# Spatial Docs (static HTML)

Drop into your repo as `spatial-docs/` and host on GitHub Pages.

```
https://soggybird0.github.io/Spatial/spatial-docs/
```

No build step. Open `index.html` locally or deploy the folder as-is.
